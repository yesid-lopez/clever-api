# from llama_index.core import Document

from study_buddy.models.file import File

# from study_buddy.utils.cloud_vision import (convert_to_readable_text,
#                                             extract_text_from_uri)


def get_documents(file: File):
    # extracted_text = extract_text_from_uri(file.uri)
    # readable_text = convert_to_readable_text(extracted_text)
    # documents = [
    #     Document(
    #         text=readable_text.text,
    #         metadata={"page_label": "1", "file_name": file.name},
    #     )
    # ]
    # return documents
    pass
